export  const CardRanking = ({ children, className }) => (
    <div className={`bg-black bg-opacity-70 rounded-lg shadow-lg ${className}`}>
      {children}
    </div>
  )  
export default CardRanking;
  